import { types } from "../types/types";

export const Cargando = () => ({type: types.cargaCargando})

export const Cargado = () => ( {type: types.cargaCargado} )

export const Cerrado = () => ({type: types.cargaClosed})